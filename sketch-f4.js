let counter = 0;



function setup() {
  createCanvas(400, 400);
}

function draw() {
  background(220);

  let angle = counter;
  
  let x = counter;
  let y = sin(angle*0.1)* 100;
  y+y + 200;
  
  if (x > width)(counter = 0)
  
  fill("red")
  ellipse(x, y, 50)

counter++
}